enum SocketCardEvents {
  CARD_ADD = "card:add",
  CARD_UPDATE = "card:update",
  CARD_DELETE = "card:delete",
  CARD_MOVE = "card:move",
}

enum SocketColumnEvents {
  COLUMN_ADD = "column:add",
  COLUMN_EDIT = "column:edit",
  COLUMN_DELETE = "column:delete",
  COLUMN_MOVE = "column:move",
}

enum SocketCommentEvents {
  COMMENT_ADD = "comment:add",
  COMMENT_DELETE = "comment:delete",
}

enum SocketLabelEvents {
  LABEL_ADD = "label:add",
  LABEL_DELETE = "label:delete",
}

enum SocketCoverEvents {
  COVER_ADD = "cover:add",
  COVER_DELETE = "cover:delete",
}
